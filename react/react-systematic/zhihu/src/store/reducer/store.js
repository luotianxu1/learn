import * as TYPES from '../action-type'

let initial = {
    list: null,
}

export default function storeReducer(state = initial, action) {
    state = { ...state }
    switch (action.type) {
        default:
    }

    return state
}
